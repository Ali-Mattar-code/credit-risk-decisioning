# Model artifacts

Run `credit-risk reproduce` to create `champion.joblib` locally. Serialized model binaries are excluded from version control because they are generated artifacts and should only be loaded from trusted sources.
